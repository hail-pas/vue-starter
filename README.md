# Develope Configure

## eslint

```shell
npm i eslint -D

npx eslint --init
touch .eslintignore
```

## prettier

```shell
npm install -D eslint-plugin-prettier prettier eslint-config-prettier

touch prettier.json
touch .prettierignore
```
