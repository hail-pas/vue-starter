// import type { EnumType } from "typescript";

// function stringToEnum<T extends EnumType, K extends keyof T>(enumType: T, value: string): T[K] | undefined {
//     return enumType[value as K];
//   }
